apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
    kubernetes.io/tls-acme: "true"
  labels:
    app: springonsk8s
  name: springonsk8s
spec:
  rules:
  - host: timmhirsens-springonsk8s.67.207.72.83.nip.io
    http:
      paths:
      - backend:
          serviceName: springonsk8s
          servicePort: 8080
        path: /
  tls:
  - hosts:
    - timmhirsens-springonsk8s.67.207.72.83.nip.io
    secretName: springonsk8s-tls
